name: Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      version:
        description: 'Release version (e.g., v0.1.0)'
        required: true
        default: 'v0.1.0'
      prerelease:
        description: 'Is this a prerelease?'
        required: true
        default: false
        type: boolean

jobs:
  build:
    name: Build ${{ matrix.os }} (${{ matrix.arch }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          # Linux
          - os: ubuntu-latest
            arch: amd64
            artifact_name: discordo
            asset_name: discordo-linux-amd64
          - os: ubuntu-latest
            arch: arm64
            artifact_name: discordo
            asset_name: discordo-linux-arm64
          # Windows
          - os: windows-latest
            arch: amd64
            artifact_name: discordo.exe
            asset_name: discordo-windows-amd64.exe
          # macOS
          - os: macos-latest
            arch: amd64
            artifact_name: discordo
            asset_name: discordo-macos-amd64
          - os: macos-15
            arch: arm64
            artifact_name: discordo
            asset_name: discordo-macos-arm64

    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version: '1.25.3'

      - name: Verify Go installation
        shell: bash
        run: |
          go version
          go env GOOS GOARCH
          echo "Runner OS: ${{ runner.os }}"
          echo "Matrix arch: ${{ matrix.arch }}"

      - name: Update Go modules
        shell: bash
        run: |
          echo "Updating go modules..."
          go mod tidy || echo "go mod tidy failed, continuing anyway"
          go mod download || echo "go mod download failed, continuing anyway"

      - name: Install dependencies (Linux)
        if: runner.os == 'Linux' && matrix.arch != 'arm64'
        run: |
          sudo apt-get update
          sudo apt-get install -y libx11-dev libasound2-dev pkg-config alsa-utils pulseaudio-utils

      - name: Set build variables
        shell: bash
        run: |
          if [ "${{ runner.os }}" = "Windows" ]; then
            echo "ARTIFACT_NAME=discordo.exe" >> $GITHUB_ENV
            echo "ASSET_NAME=discordo-windows-${{ matrix.arch }}.exe" >> $GITHUB_ENV
          else
            echo "ARTIFACT_NAME=discordo" >> $GITHUB_ENV
            echo "ASSET_NAME=discordo-${{ runner.os }}-${{ matrix.arch }}" >> $GITHUB_ENV
          fi

      - name: Build for ${{ matrix.arch }}
        shell: bash
        run: |
          set -e
          echo "Building for ${{ runner.os }} ${{ matrix.arch }}"
          
          if [ "${{ matrix.arch }}" = "arm64" ]; then
            if [ "${{ runner.os }}" = "Linux" ]; then
              # Cross-compilation for ARM64 - disable CGO and audio to avoid dependency issues
              echo "Cross-compiling Linux ARM64 with CGO_ENABLED=0"
              GOOS=linux GOARCH=arm64 CGO_ENABLED=0 go build -tags=noaudio -ldflags "-s -w" -o ${{ env.ARTIFACT_NAME }}
            elif [ "${{ runner.os }}" = "macOS" ]; then
              echo "Building macOS ARM64"
              GOOS=darwin GOARCH=arm64 go build -tags=noaudio -ldflags "-s -w" -o ${{ env.ARTIFACT_NAME }}
            fi
          else
            # For AMD64 builds
            if [ "${{ runner.os }}" = "Linux" ]; then
              echo "Building Linux AMD64 with noaudio tag"
              go build -tags=noaudio -ldflags "-s -w" -o ${{ env.ARTIFACT_NAME }}
            elif [ "${{ runner.os }}" = "Windows" ]; then
              echo "Building Windows AMD64"
              GOOS=windows GOARCH=amd64 go build -tags=noaudio -ldflags "-s -w" -o ${{ env.ARTIFACT_NAME }}
            elif [ "${{ runner.os }}" = "macOS" ]; then
              echo "Building macOS AMD64"
              GOOS=darwin GOARCH=amd64 go build -tags=noaudio -ldflags "-s -w" -o ${{ env.ARTIFACT_NAME }}
            fi
          fi
          
          echo "Build completed successfully"
          ls -la ${{ env.ARTIFACT_NAME }}

      - name: Strip binary (Unix)
        if: runner.os != 'Windows' && matrix.arch != 'arm64'
        run: strip ${{ env.ARTIFACT_NAME }}

      - name: Upload artifact
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: ${{ env.ASSET_NAME }}
          path: ${{ env.ARTIFACT_NAME }}

  create-release:
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Prepare release assets
        run: |
          mkdir -p release-assets
          cd artifacts
          # Extract binaries from artifact zip files
          for dir in */; do
            cd "$dir"
            if [[ -f discordo.exe ]]; then
              mv discordo.exe "../../release-assets/$(basename "$dir")"
            elif [[ -f discordo ]]; then
              mv discordo "../../release-assets/$(basename "$dir")"
            fi
            cd ..
          done
          cd ..
          ls -la release-assets/

      - name: Generate changelog
        id: changelog
        run: |
          # Get version from tag or input
          if [[ "${{ github.event_name }}" == "push" ]]; then
            VERSION=${GITHUB_REF#refs/tags/}
          else
            VERSION="${{ github.event.inputs.version }}"
          fi
          
          # Create changelog from CHANGELOG.md
          if [[ -f CHANGELOG.md ]]; then
            # Extract the entire unreleased section - find start and end lines
            start_line=$(grep -n "^## \[Unreleased\]$" CHANGELOG.md | cut -d: -f1)
            next_version_line=$(grep -n "^## \[" CHANGELOG.md | grep -v "Unreleased" | head -n1 | cut -d: -f1)
            
            if [ -n "$start_line" ]; then
              if [ -n "$next_version_line" ]; then
                # Extract from start_line+1 to next_version_line-1
                sed -n "$((start_line+1)),$((next_version_line-1))p" CHANGELOG.md > changelog.txt
              else
                # No next version, extract from start_line+1 to end
                sed -n "$((start_line+1)),\$p" CHANGELOG.md > changelog.txt
              fi
            else
              echo "No Unreleased section found" > changelog.txt
            fi
            
            echo "CHANGELOG<<EOF" >> $GITHUB_OUTPUT
            cat changelog.txt >> $GITHUB_OUTPUT
            echo "" >> $GITHUB_OUTPUT
            echo "## Installation" >> $GITHUB_OUTPUT
            echo "" >> $GITHUB_OUTPUT
            echo "### Download the appropriate binary for your platform:" >> $GITHUB_OUTPUT
            echo "" >> $GITHUB_OUTPUT
            echo "- **Linux AMD64**: \`discordo-linux-amd64\`" >> $GITHUB_OUTPUT
            echo "- **Linux ARM64**: \`discordo-linux-arm64\`" >> $GITHUB_OUTPUT
            echo "- **Windows AMD64**: \`discordo-windows-amd64.exe\`" >> $GITHUB_OUTPUT
            echo "- **macOS Intel**: \`discordo-macos-amd64\`" >> $GITHUB_OUTPUT
            echo "- **macOS Apple Silicon**: \`discordo-macos-arm64\`" >> $GITHUB_OUTPUT
            echo "" >> $GITHUB_OUTPUT
            echo "### Quick Start" >> $GITHUB_OUTPUT
            echo "" >> $GITHUB_OUTPUT
            echo "1. Download the binary for your platform" >> $GITHUB_OUTPUT
            echo "2. Make it executable (Linux/macOS): \`chmod +x discordo-*\`" >> $GITHUB_OUTPUT
            echo "3. Run: \`./discordo-linux-amd64\` (or appropriate binary)" >> $GITHUB_OUTPUT
            echo "" >> $GITHUB_OUTPUT
            echo "For detailed instructions, see the [README](https://github.com/ayn2op/discordo/blob/${VERSION}/README.md)" >> $GITHUB_OUTPUT
            echo "EOF" >> $GITHUB_OUTPUT
          else
            echo "CHANGELOG<<EOF" >> $GITHUB_OUTPUT
            echo "Release $VERSION" >> $GITHUB_OUTPUT
            echo "EOF" >> $GITHUB_OUTPUT
          fi

      - name: Create Release
        id: create_release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.ref_name || github.event.inputs.version }}
          name: Release ${{ github.ref_name || github.event.inputs.version }}
          body: ${{ steps.changelog.outputs.CHANGELOG }}
          draft: false
          prerelease: ${{ github.event.inputs.prerelease == 'true' || contains(github.ref_name || github.event.inputs.version, '-') }}
          files: |
            release-assets/*

      - name: Generate release summary
        run: |
          echo "## 🚀 Release Created Successfully!" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Version**: ${{ github.ref_name || github.event.inputs.version }}" >> $GITHUB_STEP_SUMMARY
          echo "**Release URL**: ${{ steps.create_release.outputs.url }}" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 📦 Assets" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          ls -la release-assets/ | while read line; do
            echo "- $line" >> $GITHUB_STEP_SUMMARY
          done